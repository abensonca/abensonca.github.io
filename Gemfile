# frozen_string_literal: true

source 'https://rubygems.org'

# GitHub Pages tracks the gem versions used by the live build, so use it
# locally too — it pins jekyll, plugins, and the safe-mode whitelist.
gem 'github-pages', group: :jekyll_plugins
gem 'webrick'
